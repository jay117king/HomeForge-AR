# Contributing

Thanks for your interest in contributing! To keep things simple for this MVP:

- Open issues for bugs or feature requests.
- Open pull requests against the `main` branch — the CI will run the build.
- Be sure to run `./gradlew assembleDebug` locally before opening a PR.
